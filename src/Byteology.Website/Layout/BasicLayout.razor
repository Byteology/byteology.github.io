@inherits LayoutComponentBase

@using Byteology.Website.Navigation

<b-viewport class="@(_state.Prerendering ? "prerendered" : "")">

    <b-layout class="@(!_state.NavigationBarRendered ? _fadeInAnimationClass : "")">

        <NavigationBar/>

        <main class="@(_state.NavigationBarRendered ? _fadeInAnimationClass : "")">
            @Body
        </main>

        <PrivacyFooter />

    </b-layout>

</b-viewport>
